import 'enum_values.dart';

enum AppThemeMode { light, dark, system }

final appThemeModeValues = EnumValues({
  "light": AppThemeMode.light,
  "dark": AppThemeMode.dark,
  "system": AppThemeMode.system,
});
